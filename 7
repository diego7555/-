#include <iostream>
using namespace std;

int main() {
    setlocale(LC_ALL, "RU"); 

    // Меню выбора месяца
    cout << "\n[ + ] Календарь\n"
        << "[ 1 ] Январь\n"
        << "[ 2 ] Февраль\n"
        << "[ 3 ] Март\n"
        << "[ 4 ] Апрель\n"
        << "[ 5 ] Май\n"
        << "[ 6 ] Июнь\n"
        << "[ 7 ] Июль\n"
        << "[ 8 ] Август\n"
        << "[ 9 ] Сентябрь\n"
        << "[ 10 ] Октябрь\n"
        << "[ 11 ] Ноябрь\n"
        << "[ 12 ] Декабрь\n"
        << "[ + ] Выберите месяц: ";

    int month;
    cin >> month;

    // Используем конструкцию switch-case для обработки введенного месяца
    switch (month) {
    case 1: // январь
        cout << "\n[ + ] Выбран месяц - Январь\n"
            << "[ + ] Выберите день месяца: ";
        break;
    case 2: // февраль
        cout << "\n[ + ] Выбран месяц - Февраль\n"
            << "[ + ] Выберите день месяца: ";
        break;
    case 3: // март
        cout << "\n[ + ] Выбран месяц - Март\n"
            << "[ + ] Выберите день месяца: ";
        break;
    case 4: // апрель
        cout << "\n[ + ] Выбран месяц - Апрель\n"
            << "[ + ] Выберите день месяца: ";
        break;
    case 5: // май
        cout << "\n[ + ] Выбран месяц - Май\n"
            << "[ + ] Выберите день месяца: ";
        break;
    case 6: // июнь
        cout << "\n[ + ] Выбран месяц - Июнь\n"
            << "[ + ] Выберите день месяца: ";
        break;
    case 7: // июль
        cout << "\n[ + ] Выбран месяц - Июль\n"
            << "[ + ] Выберите день месяца: ";
        break;
    case 8: // август
        cout << "\n[ + ] Выбран месяц - Август\n"
            << "[ + ] Выберите день месяца: ";
        break;
    case 9: // сентябрь
        cout << "\n[ + ] Выбран месяц - Сентябрь\n"
            << "[ + ] Выберите день месяца: ";
        break;
    case 10: // октябрь
        cout << "\n[ + ] Выбран месяц - Октябрь\n"
            << "[ + ] Выберите день месяца: ";
        break;
    case 11: // ноябрь
        cout << "\n[ + ] Выбран месяц - Ноябрь\n"
            << "[ + ] Выберите день месяца: ";
        break;
    case 12: // декабрь
        cout << "\n[ + ] Выбран месяц - Декабрь\n"
            << "[ + ] Выберите день месяца: ";
        break;
    default:
        cout << "\nОшибка: такого месяца нет!\n";
        return 1; // Завершаем программу, если введен неверный месяц
    }

    // Читаем ввод дня
    int day;
    cin >> day;

    // Выводим итоговую строку календаря
    switch (month) {
    case 1: // январь
        if (day >= 1 && day <= 31)
            cout << "\n[ + ] Календарь: " << day << " января.\n";
        else
            cout << "\nОшибка: такого дня нет в январе!\n";
        break;
    case 2: // февраль
        if (day >= 1 && day <= 28)
            cout << "\n[ + ] Календарь: " << day << " февраля.\n";
        else
            cout << "\nОшибка: такого дня нет в феврале!\n";
        break;
    case 3: // март
        if (day >= 1 && day <= 31)
            cout << "\n[ + ] Календарь: " << day << " марта.\n";
        else
            cout << "\nОшибка: такого дня нет в марте!\n";
        break;
    case 4: // апрель
        if (day >= 1 && day <= 30)
            cout << "\n[ + ] Календарь: " << day << " апреля.\n";
        else
            cout << "\nОшибка: такого дня нет в апреле!\n";
        break;
    case 5: // май
        if (day >= 1 && day <= 31)
            cout << "\n[ + ] Календарь: " << day << " мая.\n";
        else
            cout << "\nОшибка: такого дня нет в мае!\n";
        break;
    case 6: // июнь
        if (day >= 1 && day <= 30)
            cout << "\n[ + ] Календарь: " << day << " июня.\n";
        else
            cout << "\nОшибка: такого дня нет в июне!\n";
        break;
    case 7: // июль
        if (day >= 1 && day <= 31)
            cout << "\n[ + ] Календарь: " << day << " июля.\n";
        else
            cout << "\nОшибка: такого дня нет в июле!\n";
        break;
    case 8: // август
        if (day >= 1 && day <= 31)
            cout << "\n[ + ] Календарь: " << day << " августа.\n";
        else
            cout << "\nОшибка: такого дня нет в августе!\n";
        break;
    case 9: // сентябрь
        if (day >= 1 && day <= 30)
            cout << "\n[ + ] Календарь: " << day << " сентября.\n";
        else
            cout << "\nОшибка: такого дня нет в сентябре!\n";
        break;
    case 10: // октябрь
        if (day >= 1 && day <= 31)
            cout << "\n[ + ] Календарь: " << day << " октября.\n";
        else
            cout << "\nОшибка: такого дня нет в октябре!\n";
        break;
    case 11: // ноябрь
        if (day >= 1 && day <= 30)
            cout << "\n[ + ] Календарь: " << day << " ноября.\n";
        else
            cout << "\nОшибка: такого дня нет в ноябре!\n";
        break;
    case 12: // декабрь
        if (day >= 1 && day <= 31)
            cout << "\n[ + ] Календарь: " << day << " декабря.\n";
        else
            cout << "\nОшибка: такого дня нет в декабре!\n";
        break;
    default:
        cout << "\nОшибка: неизвестный месяц!\n"; 
        break;
    }

    return 0;
}
